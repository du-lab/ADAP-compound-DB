variables:
#  MAVEN_EXEC: C:/Maven/apache-maven-3.6.3/bin/mvn
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"
  HOMEPAGE_URL: http://localhost:8080/
  CONTAINER_NAME: adap-kdb-container

# In addition, variables ADAP_KDB_TEST_USERNAME and ADAP_KDB_TEST_PASSWORD must be defined in GitLab CI settings.
cache:
  paths:
    - .m2/repository

build:
  stage: build
  only:
    - master
    - merge_requests
  script:
    - mvn -version
    - mvn clean install
  artifacts:
    paths:
      - target/
    expire_in: 1 hour
  timeout: 15m
  tags:
    - toan-macbookpro

test:
  stage: test
  needs: ["build"]
  only:
    - master
    - merge_requests
  # Reuse downloaded browser binaries and Python packages between runs
  # so the runner does not re-download everything every time.
  cache:
    key: chrome-for-testing-mac-arm64
    paths:
      - .tools/chrome-for-testing/
      - venv/

  script:
    - docker rm --force $CONTAINER_NAME || true
    # Remove dangling image
    - docker system prune --force

    # Create an isolated Python environment for Selenium test dependencies.
    - python3 -m venv venv
    - venv/bin/pip install --upgrade pip
    - venv/bin/pip install --upgrade selenium

    # Download Chrome for Testing + matching ChromeDriver for arm64
    # only if they are not already cached.
    - mkdir -p .tools/chrome-for-testing
    - |
      if [ ! -x .tools/chrome-for-testing/chromedriver-mac-arm64/chromedriver ]; then
        cd .tools/chrome-for-testing
        VERSION=$(curl -s https://googlechromelabs.github.io/chrome-for-testing/LATEST_RELEASE_145.0.7632)
        echo "Using Chrome for Testing version: $VERSION"
        curl -LO "https://storage.googleapis.com/chrome-for-testing-public/${VERSION}/mac-arm64/chrome-mac-arm64.zip"
        curl -LO "https://storage.googleapis.com/chrome-for-testing-public/${VERSION}/mac-arm64/chromedriver-mac-arm64.zip"
        unzip -o chrome-mac-arm64.zip
        unzip -o chromedriver-mac-arm64.zip
        chmod +x chromedriver-mac-arm64/chromedriver
        cd "$CI_PROJECT_DIR"
      fi

    - docker build -f docker/adap-kdb-docker/Dockerfile -t adap-kdb-img .
    - docker run --name $CONTAINER_NAME -e DATABASE=cci-dulab.uncc.edu -e INTEGRATION_TEST=true -d -p8080:8080 adap-kdb-img
    - sleep 20
    - venv/bin/python -c "import selenium; print(selenium.__version__)"
    - .tools/chrome-for-testing/chromedriver-mac-arm64/chromedriver --version

    - venv/bin/python scripts/selenium/initial_test.py --homepage-url "$HOMEPAGE_URL"
    - venv/bin/python scripts/selenium/ontology_level_test.py --homepage-url $HOMEPAGE_URL --username $ADAP_KDB_TEST_USERNAME --password $ADAP_KDB_TEST_PASSWORD
    - venv/bin/python scripts/selenium/manual_search_test.py --homepage-url $HOMEPAGE_URL --identifier "eicosatrienoic acid" --spectrum "79 100; 67 90.3325; 80 75.8722; 81 59.0838; 93 54.0933"
    - venv/bin/python scripts/selenium/msp_upload_process_test.py --homepage-url $HOMEPAGE_URL --msp-path $CI_PROJECT_DIR/scripts/selenium/combined.msp
    - venv/bin/python scripts/selenium/login_register_test.py --homepage-url $HOMEPAGE_URL
    - venv/bin/python scripts/selenium/individual_search_test_without_login.py --homepage-url $HOMEPAGE_URL --msp-path $CI_PROJECT_DIR/scripts/selenium/combined.msp
    - venv/bin/python scripts/selenium/individual_search_test_after_login.py --homepage-url $HOMEPAGE_URL --msp-path $CI_PROJECT_DIR/scripts/selenium/combined.msp --user-name $ADAP_KDB_TEST_USERNAME --user-password $ADAP_KDB_TEST_PASSWORD
    - venv/bin/python scripts/selenium/group_search_test_without_login.py --homepage-url $HOMEPAGE_URL --msp-path $CI_PROJECT_DIR/scripts/selenium/combined.msp
    - venv/bin/python scripts/selenium/cluster_page_test.py --homepage-url $HOMEPAGE_URL
    - venv/bin/python scripts/selenium/upload_and_save_study_after_login.py --homepage-url $HOMEPAGE_URL --msp-path $CI_PROJECT_DIR/scripts/selenium/combined.msp  --user-name $ADAP_KDB_TEST_USERNAME --user-password $ADAP_KDB_TEST_PASSWORD
    - venv/bin/python scripts/selenium/organization_test.py --homepage-url $HOMEPAGE_URL

  timeout: 45m
  after_script:
    - docker rm --force $CONTAINER_NAME
  tags:
    - toan-macbookpro


package:
  stage: deploy
  script:
    - docker save adap-kdb-img  > adap-kdb-img.tar
    - mkdir artifact
    - cp adap-kdb-img.tar artifact/
    - cp scripts/script.sh artifact/
  artifacts:
    paths:
      - "artifact/*"
    name: "adap-kdb-img"
    expire_in: 1 day
  only:
    - master
    - merge_requests
  timeout: 15m
  tags:
    - toan-macbookpro

